# `memr` Example With Toggling Progress Bar

Each command below can also use `--output=<FILE>` to specify the output file (default=`output.memr`)

## with progress (default)

    go run . --progress

## without progress (default)

    go run . --progress=false
