# `memr` Example With Custom Header

Each command below can also use `--output=<FILE>` to specify the output file (default=`output.memr`)

## raw (no header)

    go run . --raw

## custom magic (default=MEMR)

    go run . --magic=HEYO
